// Main entry point for the quiz application
console.log('Quiz application loaded');

// Since this is a static HTML application, we don't need complex bundling
// Just ensure all scripts are loaded properly